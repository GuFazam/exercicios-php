<?php
$cores = array("vermelho", "amarelo", "azul", "verde", "roxo");
echo "Existem " . count($cores) . " itens " . "neste array";

//usando foreach

// $cores = array("vermelho", "amerelo", "azul", "verde", "roxo");

// Inicializar uma variável contador
#$quantidade = 0;

// Percorrer o array e incrementar o contador
#foreach ($cores as $cor) {
#    $quantidade++;
#}

// Imprimir a quantidade de elementos no array
#echo $quantidade;
?>